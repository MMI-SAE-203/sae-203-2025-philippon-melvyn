---
import { getImageUrl } from "../js/backend.mjs";
const { Activites } = Astro.props;
const AfficheUrl = getImageUrl(Activites, "image");
---

<div class="bg-white shadow-md rounded-lg overflow-hidden">
    <img
        src={`http://127.0.0.1:8090/api/files/Activites/${Activites.id}/${Activites.Affiche_sportive}`}
        alt={Activites.Titre}
        class="w-full h-48 object-cover"
        loading="lazy"
    />
    <div class="p-4">
        <p class="text-xl font-semibold text-gray-800">{Activites.Titre}</p>
        <p class="text-gray-600 mt-2">{Activites.Description}</p>
        <a
            href={`/activites/${Activites.id}`}
            class="inline-block mt-4 text-indigo-600 hover:text-indigo-800"
        >
            Voir les détails
        </a>
    </div>
</div>
